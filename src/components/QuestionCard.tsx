

export default function QuestionCard(){
    return(
        <div className="question-card">
            {/**split between:
             * header, 
             * question body, 
             * answer choices,
             * footer/nav  
             * 
             * each of these will be dynamic inputs from parent function
             * */}
        </div>
    )
}